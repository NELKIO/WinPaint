name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-macos:
    name: Build macOS (${{ matrix.label }})
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - runner: macos-15-intel
            label: Intel
          - runner: macos-latest
            label: AppleSilicon
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install PyQt5 pyinstaller

      - name: Build
        run: bash scripts/build_macos.sh

      - name: Rename artifact
        run: mv dist/WinPaint-macOS.zip dist/WinPaint-macOS-${{ matrix.label }}.zip

      - uses: actions/upload-artifact@v4
        with:
          name: macos-${{ matrix.label }}
          path: dist/WinPaint-macOS-${{ matrix.label }}.zip

  build-linux:
    name: Build Linux (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - runner: ubuntu-latest
            arch: x86_64
          - runner: ubuntu-24.04-arm
            arch: aarch64
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install PyQt5 pyinstaller

      - name: Build
        run: bash scripts/build_linux.sh

      - uses: actions/upload-artifact@v4
        with:
          name: linux-${{ matrix.arch }}
          path: dist/WinPaint-Linux-${{ matrix.arch }}.tar.gz

  build-windows:
    name: Build Windows (x64)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install PyQt5 pyinstaller pillow

      - name: Create .ico icon
        run: |
          python -c "from PIL import Image; img = Image.open('assets/icon_256.png'); img.save('WinPaint.ico', sizes=[(16,16),(32,32),(48,48),(64,64),(128,128),(256,256)])"

      - name: Build
        run: >
          pyinstaller --noconfirm --onefile --windowed --name WinPaint
          --icon WinPaint.ico
          --add-data "assets;assets"
          --hidden-import PyQt5.sip
          --collect-all PyQt5
          src/run.py

      - name: Rename artifact
        run: Move-Item dist/WinPaint.exe dist/WinPaint-Windows-x64.exe

      - uses: actions/upload-artifact@v4
        with:
          name: windows
          path: dist/WinPaint-Windows-x64.exe

  release:
    name: Publish release
    needs: [build-macos, build-linux, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: artifacts

      - name: Create release and upload assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --repo "${GITHUB_REPOSITORY}" \
            --title "WinPaint ${GITHUB_REF_NAME}" \
            --generate-notes \
            artifacts/*
